<h1>New Issue</h1>

<%= form_with model: [ @project, @project.issues.build ]  do |f| %>
  <%= f.label :Title %>
  <%= f.text_field :Title %>

  <%= f.label :Severity %>
  <%= f.select :Severity, ['low', 'medium', 'high'] %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit %>
<% end %>
